export type FormFields = {
  query: string;
};

export enum Label {
  FIELD_QUERY = "Search query",
  HELP = "Help",
  SEARCH = "Search",
  COPY_JSON = "Copy JSON",
}

export enum TestId {
  SEARCH_FORM = "search-form",
}
