export enum Label {
  OUTPUT = "Output",
  FETCH_ERROR = "Error while trying to fetch data.",
}

export enum Output {
  ALL = "STDOUT/STDERR",
  STDERR = "STDERR",
  STDOUT = "STDOUT",
}

export enum TestId {
  SHOW_OUTPUT = "show-output",
}
