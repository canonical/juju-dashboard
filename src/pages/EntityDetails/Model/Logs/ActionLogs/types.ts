export enum Label {
  OUTPUT = "Output",
  COPY = "Copy to clipboard",
  FETCH_ERROR = "Error while trying to fetch data.",
}

export enum Output {
  ALL = "STDOUT/STDERR",
  STDERR = "STDERR",
  STDOUT = "STDOUT",
}
