export enum Label {
  CANCEL_BUTTON = "Cancel",
  CONFIRM_BUTTON = "Confirm",
  EXECUTE_ACTION_ERROR = "Couldn't start the action.",
}

export enum TestId {
  MODEL_UNIT_COUNT = "confirmation-modal-unit-count",
  MODEL_UNIT_NAMES = "confirmation-modal-unit-names",
}
