export type FormFields = {
  removeAll: boolean;
  revision: string;
};

export enum TestId {
  PANEL = "remove-secret-panel",
}

export enum Label {
  CANCEL = "Cancel",
  SUBMIT = "Remove secret",
}
