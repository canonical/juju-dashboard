/**
 * This is a function.
 *
 * @param {string} str - A string param
 * @param {string} character on which to split string
 * @return {array} Array of strings split
 */
export function splitStr(str, delimiter) {
  return str.split(delimiter);
}
