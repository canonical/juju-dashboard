import { ReactElement } from "react";

import type { ConfigProps } from "./ConfigPanel";

export default function BooleanConfig({ config }: ConfigProps): ReactElement {
  return <div className="config-input"></div>;
}
