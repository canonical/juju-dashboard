credentials:
  localhost:
    default-region: localhost
    {{username}}:
      auth-type: interactive
      trust-password: {{password}}
  microk8s:
    {{username}}:
      auth-type: userpass
      username: {{username}}
      password: {{password}}
