[project]
name = "juju-dashboard-k8s"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "ops == 3.8.1",
  "pydantic == 2.13.4",
  "common @ file:///home/ubuntu/code/juju-dashboard/charms/common",
]

[project.optional-dependencies]
test = [
  "coverage",
  "flake8",
  "ops[testing]",
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
[tool.poetry.dependencies]
common = {develop = true}
