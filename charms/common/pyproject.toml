[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.10"
authors = []
dependencies = [
  "jinja2 == 3.1.6",
  "ops == 3.8.1",
]

[dependency-groups]
dev = [
  "coverage",
  "flake8",
  "ops[testing]",
]

[build-system]
requires = ["uv_build>=0.12.6,<0.13"]
build-backend = "uv_build"
