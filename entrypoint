#! /usr/bin/env bash

# When deploying the dashboard charm we need to update the configurations whenever 
# the relation with the controller changes, so they are generated by the charm
# and mouted into `/srv/configs` on the container
cp /srv/configs/config.js /srv/config.js 2>/dev/null
cp /srv/configs/nginx.conf /etc/nginx/sites-available/default 2>/dev/null

nginx -g "daemon off;"
